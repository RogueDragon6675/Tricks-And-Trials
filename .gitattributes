# Default: normalize all text files to LF
* text=auto eol=lf

# Explicitly binary — never touch these
*.png binary
*.jpg binary
*.jar binary
*.zip binary
*.ogg binary
*.wav binary

# Windows scripts keep CRLF
*.bat text eol=crlf
*.cmd text eol=crlf